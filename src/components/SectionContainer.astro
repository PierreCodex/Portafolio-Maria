---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section transition-all duration-700 ease-out will-change-transform will-change-opacity ${className} scroll-m-4 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>
